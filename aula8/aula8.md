# Aula 8 — Módulos e Packages

Este arquivo contém a explicação teórica sobre módulos e packages.

Resumo: 
- Módulo = arquivo Python
- Package = pasta com `__init__.py` que agrupa módulos

Confira `exemplos.py` para ver código executável e `tests/` para verificar os testes.